import React from 'react';
import {StyleSheet, View} from 'react-native';

const ABC = (props) => {
    return (
        <View>

        </View>
    );
};

const styles = StyleSheet.create({
    home: {
        flex: 1,
    }
});

export default ABC;
